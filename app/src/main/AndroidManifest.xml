<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
        <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- Para volver a arrancar tras reiniciar el móvil. Sin esto, un reinicio
         —incluido el que hace Android solo al actualizarse— dejaba parado para
         siempre un aparato que llevaba días trabajando para un cluster, y lo
         único que se veía era que desaparecía de la lista del maestro. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Sólo para leer el QR de la dirección al enviar (QrScanActivity). -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera.any" android:required="false" />

    <!-- Desde Android 11 una app no ve qué otras hay instaladas salvo que las
         declare aquí. Sin esto, getPackageInfo("com.tailscale.ipn") lanza
         NameNotFoundException AUNQUE Tailscale esté instalado, así que el botón
         diría "Instalar Tailscale" a quien ya lo tiene y le mandaría a la
         tienda. Y getLaunchIntentForPackage devolvería null, o sea que tampoco
         podría abrirlo.
         targetSdk es 34, así que el filtrado aplica. -->
    <queries>
        <package android:name="com.tailscale.ipn" />
    </queries>
    <application
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="Wallet Hunter"
        android:theme="@style/AppThemeDark"
        android:requestLegacyExternalStorage="true">
        <activity android:name=".StatsActivity" android:screenOrientation="portrait" />
        <activity android:name=".WalletActivity"
            android:theme="@style/AppThemeWallet"
            android:screenOrientation="portrait" />
        <activity android:name=".WelcomeActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- exported="true" sin intent-filter: cualquier app instalada podía
             abrir MainActivity por nombre de componente y saltarse la pantalla
             de PIN de WelcomeActivity, que es el único sitio donde se pide.
             Sólo la arrancan WelcomeActivity, el widget y la notificación del
             servicio, todos dentro de la app. -->
        <activity android:name=".MainActivity" android:exported="false"
            android:launchMode="singleTop"
        android:screenOrientation="portrait"
        android:configChanges="orientation|screenSize|keyboardHidden">
        </activity>
        <service android:name=".HunterService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- exported="true" es obligatorio aquí: quien manda estos avisos es el
             propio Android, o sea un proceso de fuera. El receptor no decide
             nada por su cuenta —sólo levanta el servicio, que mira en
             preferencias si había algo que recuperar—, así que una app ajena
             que lo dispare no consigue nada que no pudiera conseguir abriendo
             la app. -->
        <receiver android:name=".BootReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
        <activity android:name=".DebugActivity"
            android:theme="@style/AppThemeDark" />

        <activity android:name=".NetworkActivity"
            android:theme="@style/AppThemeDark" />

        <activity android:name=".QrScanActivity"
            android:theme="@style/AppThemeDark"
            android:screenOrientation="portrait" />

        <activity android:name=".VaultActivity"
            android:theme="@style/AppThemeDark"
            android:screenOrientation="portrait" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
