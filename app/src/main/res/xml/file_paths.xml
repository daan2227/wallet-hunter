<?xml version="1.0" encoding="utf-8"?>
<!-- Sólo las dos carpetas desde las que se comparte (BackupStore). Antes
     path="." en el almacenamiento interno y en el externo: la raíz entera. -->
<paths>
    <files-path name="backups" path="backups/" />
    <files-path name="shared" path="shared/" />
</paths>
